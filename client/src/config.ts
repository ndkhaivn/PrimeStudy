// Note: Using .ts instead of .json file give us type annotation
const config = {
    i18n: {
        dir: '/assets/i18n',
        defaultLang: 'vn',
        langs: [
            'en', 'vn',
        ],
   },
};

export default config;
